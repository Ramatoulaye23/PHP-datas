

fopen(
    string $filename,
    string $w+,
    bool $use_include_path = false,
    ?resource $context = null
): resource|FALSE

1 ouvir le fichier fonction fopen:  
2 faire boucle avec fgetcsv
3 ajouter les lignes dans le datas
4 fermer avec fclose()


// verifier si le fichier existe 
file_exists(string $filename): bool

// ouvrir fichier avec url:

////////slug///////// est une presentation du vrai nom d'un mot
tous les caracteres speciaux enlever
exemple besançon devient besancon
les remplacent par des tiret(-)
le slug est unique :

//////soundex/////// representation de la sonorisation d'un mot//////////
=> enlever les espace: str_replace epace par representationmettre en majuscule
=> conserver la premiere lettre
=> supprimer les voyelles a e h i o u w y(different de la 1ere lettre)
=> attribuer une valeur numérique aux lettres representationmettre
=> si deux lettres ou plus avec le meme nombre adjacentes on considère qu ils fond 1 seulle lettre
//////////////////fin soundex//////////


///////attention a ne pas confondre = simple et == double////
= assigne une valeur
== compare si c'est egale
exemple
if ($success==false ) est différent de if ($success=false ) qui donne la valeur false a notre variable
 pour contrer ce probleme il faut: faire le contraire appeler methode yoda
 /////yoda/////////
if (false= $success)
condition avec yodaa
if (false= $success) donnera certainement une erreur