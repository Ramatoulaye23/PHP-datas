<?php
/**
 * Lit puis retourne le contenu d'un fichier XML
 *
 * @param string $filename Fichier à lire
 * @return array Tableau contenant les données du fichier
 */
function readXMLFile(string $filename) : array {
 
    return [];
}